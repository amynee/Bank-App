server:
  port: 9191
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bank_db_qa
    username: postgres
    password: asser1ssora1
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true